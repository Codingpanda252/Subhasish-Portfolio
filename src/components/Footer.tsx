import React from 'react';

const Footer = () => (
  <footer className="footer">
    &copy; {new Date().getFullYear()} Subhasish Panda. Built with ❤️ using React + Tailwind CSS.
  </footer>
);

export default Footer;
